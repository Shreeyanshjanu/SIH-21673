package com.sih.voicebridge.network

class ConnectionManager {
    fun startServer(port: Int) {
        // Reserved for native TCP/Wi-Fi migration in later phases.
    }

    fun connect(host: String, port: Int) {
        // Reserved for native TCP/Wi-Fi migration in later phases.
    }

    fun disconnect() {
        // Reserved for native TCP/Wi-Fi migration in later phases.
    }
}

